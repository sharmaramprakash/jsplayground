export const capitalize = (a) => a.toUpperCase();

export let firstValue = 20;
export let lastValue = 50;

export default ((a,b) => a+b); // In case of default export there is no need to give any name of a funtion, it is understood.